import type {Config} from "tailwindcss";

export default {
  darkMode: ["class"],
  content: [
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      screens: {
        // Mobile phones
        xs: "320px", // Small phones
        sm: "375px", // iPhone SE / Small devices
        md: "414px", // Larger phones like iPhone Plus models

        // Samsung Z Fold
        fold: "653px", // Samsung Galaxy Z Fold unfolded resolution

        // iPad sizes
        "ipad-mini": "768px", // iPad Mini / iPad Air portrait
        ipad: "834px", // iPad Pro 11-inch
        "ipad-pro": "1024px", // iPad Pro 12.9-inch portrait

        // Nest Hub
        nesthub: "1024px", // Google Nest Hub Max

        // Laptops
        "laptop-sm": "1280px", // Small laptops
        "laptop-md": "1440px", // Medium laptops
        "laptop-lg": "1680px", // Large laptops

        // Desktop / 4K
        desktop: "1920px", // Standard desktops / Full HD
        "4k": "3840px", // 4K resolution
      },
      colors: {
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        chart: {
          "1": "hsl(var(--chart-1))",
          "2": "hsl(var(--chart-2))",
          "3": "hsl(var(--chart-3))",
          "4": "hsl(var(--chart-4))",
          "5": "hsl(var(--chart-5))",
        },
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
} satisfies Config;
